load img = "Brats17_2013_17_1_flair.nii.gz"

let thr(x) = (200 .<= intensity(x))
let phi(x) = (900 .<= intensity(x))

//let ccthr(x) = lcc(near(thr(x)))
//let b(x) = ccthr(ccthr(x))
//let c(x) = b(b(x))
let t = thr(img)
let p = phi(img)

//save "output/lcc.png" b(c(c(c(c(img)))))
//save "output/thr.nii.gz" thr(img)
//save "output/phi.nii.gz" phi(img)
save "output/through.nii.gz" through(t, p)