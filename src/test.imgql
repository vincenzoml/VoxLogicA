let video = load("video.avi")

let nFrames = 10

let interesting = gt(intensity(frame(video,n,nFrames)),42)
let moreInteresting = gt(intensity(frame(video,n,nFrames)),52)

save "result" until(interesting,moreInteresting)
