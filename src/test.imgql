load graph = "testGraph.json"

// L'operatore "b ~> a" è vero sui punti x per cui esiste un path p da x a y, tale che p[0] |= b, p[y] |= a, e tutti i punti intermedi in (0,y) soddisfano b. 

let rho(a,b) = bnear((b ~> bnear(a)) | a) 

let red = ap("red")
let blue = ap("blue")

// save "red.json" red
// save "near_red.json" near(red)
save "nred_reach_red.json" through(not(red),red)
save "blue_reach_red.json" through(blue,red)
