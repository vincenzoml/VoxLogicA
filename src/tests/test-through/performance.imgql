load img = "Brats17_2013_17_1_flair.nii.gz"

let a = 900 .<= intensity(img)
let I(x) = through(x,x)
let F(x) = (N x) & x
let R(x) = I I F x
let S(x) = R R R R x
let Q(x) = S S x

save "output/through.nii.gz" F a

