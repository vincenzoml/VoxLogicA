load img = "Brats17_2013_17_1_flair.nii.gz"

let a = 900 .<= intensity(img)
let F(x) = (N x) & (x | x)
let I(x) = through(x,F x)
let R(x) = I I F F F F x
let S(x) = R R R R x
let Q(x) = S S R x

save "output/a.nii.gz" Q a

