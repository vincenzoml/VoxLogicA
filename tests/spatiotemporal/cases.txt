# One line per test case: <name> <numFrames> [<maxLabels>]
# The bound on the labels is passed to the first pass as --maxlabels, and only
# the cases that quantify over labels need one. The expected output of each case
# is in expected/<name>.golden and is produced by run.sh --update. Cases whose specification is marked KNOWN BAD pin the
# current, wrong behaviour of an open bug: when the bug is fixed the diff is
# expected, and the golden file has to be regenerated on purpose.

until-final           10
until-composed         3
until-twice            2
until-nested           2
diamond-nested         3
sharing                2
literals               2
horizon-overflow       2
nullary-identifier     2
wrong-context-name     2
initially-nested       3

# The existential over labels
exists-footprint       3  2
free-label             2  2
self-bound-labelling   2  2

# The merge series in frames/, where footprint labelling and propagation differ
merge-footprint        3  2
merge-initially        3  2

# Label propagation (branch existential-labels): tracked is not implemented yet
tracked-label          3
exists-label           3  2
merge-tracked          3
