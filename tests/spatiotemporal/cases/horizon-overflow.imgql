// More next operators than there are frames: the specification looks four frames
// ahead but is unrolled over two. Past the end the last frame persists, so the
// frames beyond the second repeat it, and the run warns about it.
let video = load("video.avi")
let interesting = gt(intensity(frame(video,n)),42)
save "result" diamond(diamond(diamond(diamond(interesting))))
