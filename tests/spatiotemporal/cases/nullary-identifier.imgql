// An identifier applied to no arguments, both on its own and as an argument:
// the partial evaluator has to emit it without an empty argument list, since
// "foo()" is not a term of the language.
let x = foo
save "result" bar(x)
