// KNOWN BAD (bug 7): the frame reference is called m, but the pipeline is run
// with --providecontext n. The mistake travels through two passes unnoticed and
// surfaces at the third as "unbound value", with no position and no identifier.
let video = load("video.avi")
let interesting = gt(intensity(frame(video,m)),42)
save "result" diamond(interesting)
